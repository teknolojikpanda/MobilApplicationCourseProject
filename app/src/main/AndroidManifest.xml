<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_project"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".SelectPlaylist"></activity>
        <activity android:name=".CreatePlaylist" />
        <activity android:name=".PlaylistActivity" />
        <activity android:name=".MainMusicPlayer" />
        <activity android:name=".AlbumActivity" />
        <activity android:name=".LibraryMenu" />
        <activity android:name=".SearchMenu" />
        <activity android:name=".Settings" />
        <activity android:name=".Menu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>